Form:
-----------
1. template driven form:
	use some directives to manage the form controls and the form itself
	package: @angular/forms
	module: FormsModule
	directives: ngModel, ngForm, ....
	
	@Directive({selector:'[ngModel]'})
	export class NgModelDirective{}

	@NgModule({
		declarations:[NgModelDirective],
		exports:[NgModelDirective]
	})
	export class FormsModule{
	}

2. reactive form
	control the form controls and the form itself from the code NOT the template

	package: @angular/forms
	module: ReactiveFormsModule
	directives: formControl, formControlName, formGroup,...
	
	@Directive({selector:'[formControl]'})
	export class FormControlDirective{}

	@NgModule({
		declarations:[FormControlDirective],
		exports:[FormControlDirective]
	})
	export class ReactiveFormsModule{
	}

3. signal-based form

class Validators{
	static required(control:AbstractControl<any,any,any>):ValidationErrors|null{
		if(control.value===''){
			return {
				required: true
			}
		}
		return null;
	}
	static maxLength(control:AbstractControl<any,any,any>,val:number):ValidationErrors|null{
		if(control.value.length>val){
			return {
				maxLength: true
			}
		}
		return null;
	}
}
interface ValidationErrors{
	[key:string]:any;
}

/login -> /products
/products/add -> /login?returnUrl=/products/add

RouterState:
This is a tree of activated route objects. Every node in this tree knows about the "consumed" URL segments, the extracted parameters, and the resolved data.


RouterStateSnapshot: 
This is a tree of activated route snapshots. Every node in this tree knows about the "consumed" URL segments, the extracted parameters, and the resolved data.




